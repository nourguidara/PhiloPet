import React from'react';
import Login from '../Components/Login';

const LoginPage = () => {
  return (
    <div>
      <Login /> {/* This renders the Home component */}
    </div>
  );
};

export default LoginPage;
